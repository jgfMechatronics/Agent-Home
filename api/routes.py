"""API routes — Section 4.1 (stubs for TDD).

These are minimal stubs to make tests importable
"""
from typing import Any


def map_to_sse(event: Any) -> dict:
    """Convert a Pydantic AI streaming event to SSE-compatible dict. Stub."""
    raise NotImplementedError("map_to_sse not implemented")
